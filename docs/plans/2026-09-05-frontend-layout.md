# 全站前端布局优化

## 范围与基线

- 基线提交：`a279b97`，前端工作树开工 clean。
- 保留国家内容、官方 URL、费用、核验日期、校验规则、决策数据、配色和实际系统字体栈。
- `assets/fonts/README.md` 明确当前使用系统字体；项目入口文档的“已自托管”描述已过时。
- 全量浏览器基线：314 个实际渲染页面（含两种语言的 404，排除重定向）× 390/768/1440px，
  共 942 组合；255 组合整页溢出，其中手机 249、平板 6。
- 62 页含校验器、626 个输入框；390px 时最小宽度 28px。
- 100 个字段指南页共 954 张字段卡，768px 时说明列宽 152–230px，示例固定 380px。
- 证据：`test-results/frontend-audit/before.json` 与同目录截图，均为被 Git 忽略的本地产物。

## 实施顺序

1. 修复共用长文本、官方链接块与表单布局，保证手机输入框和错误提示可读。
2. 字段说明/示例按断点重排，错误说明取消固定窄列；首页费用与变更记录使用可收缩列。
3. 恢复现有 `max-w-none` 的实际作用，区分正文阅读宽度和工具可用宽度；统一文章侧栏比例、
   首屏标题尺寸与留白，避免在平板提前挤出多行。
4. 补齐键盘焦点与跳过导航入口；Markdown 表格保持原生语义，并在表格区域内部滚动。
5. 再次扫描所有 314 页，检查截图与实际交互；独立评审 diff 后，按本会话已有授权提交发布。

## 验证门槛

- `build:prod`、`check:seo`、`git diff --check` 通过。
- 全量 942 组合整页无横向溢出，允许显式表格/代码滚动容器内部滚动。
- 对比正文与链接，内容、费用和核验数据不得丢失或改写。
- 320/390px 校验输入可用；错误提示和标签同组可见；768px 字段说明具有实际阅读空间。
- 目录搜索、首页搜索、国家/语言菜单、真实校验器函数、决策树所有可达分支验证。
- 临时恢复基线源码后，新增回归断言必须失败；恢复最终源码后通过。
- 独立评审与主控复验、CI、production deployment SHA、正式页面交互分开记录。

## 评估参考

- [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md)：
  关注焦点、长内容、输入标签、页面缩放和语义元素；不引入与本任务无关的新交互系统。
- [Hugo 表格 render hook](https://gohugo.io/render-hooks/tables/)：保留表格单元格内容、对齐与语义，
  只增加窄屏滚动容器。
